<% unless current_user?(@user) %>
  <div id="follow_form">
    <% if current_user.users.following?(@user) %>
      <%= render t "views.users.follow_form.unfollow" %>
    <% else %>
      <%= render t "views.users.follow_form.follow" %>
    <% end %>
  </div>
<% end %>
